# KISI BHI branch par push = ye chalega. Branch filter nahi.
name: On any push

on:
  push:

jobs:
  log:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Push hua - branch ${{ github.ref_name }}, commit ${{ github.sha }}"