# Test: KISI BHI branch par push hone par chalega - branch name log me dikhega
name: Test push trigger

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Kaunsi branch par push hua
        run: |
          echo "Branch = ${{ github.ref_name }}"
          echo "Ref = ${{ github.ref }}"
          echo "Agar ye run dikha to push trigger kaam kar raha hai, lekin branch main/master nahi thi"
